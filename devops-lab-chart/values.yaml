image:
  repository: ghcr.io/ronmark1/devops-lab-app     # switches to a GHCR path in Phase 5
  tag: "latest"
  pullPolicy: Always
replicaCount: 2
service:
  port: 80
  targetPort: 8080
# monitoring toggle — flipped on in Phase 6
serviceMonitor:
  enabled: false
